from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict the number of leaves on a plant given an image of the plant.',
    author='Ian Scott Knight',
    license='',
)
